<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/whimsical_background"
    android:padding="8dp">

    <!-- BIG VIDEO AREA -->
    <VideoView
        android:id="@+id/videoViewSong"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/videoControlsRow" />

    <!-- PLAY / PAUSE / STOP (LOWER ON SCREEN) -->
    <LinearLayout
        android:id="@+id/videoControlsRow"
        android:layout_width="342dp"
        android:layout_height="81dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        app:layout_constraintBottom_toTopOf="@id/bottomNavRow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageButton
            android:id="@+id/btnVideoPlay"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_margin="8dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@null"
            android:drawableTint="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/button_play" />

        <ImageButton
            android:id="@+id/btnVideoPause"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_margin="8dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@null"
            android:drawableTint="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/button_pause" />

        <ImageButton
            android:id="@+id/btnVideoStop"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_margin="8dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@null"
            android:drawableTint="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/button_stop" />
    </LinearLayout>

    <!-- BOTTOM ROW (EVEN LOWER) -->
    <LinearLayout
        android:id="@+id/bottomNavRow"
        android:layout_width="397dp"
        android:layout_height="66dp"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageButton
            android:id="@+id/btnVideoClose"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@null"
            android:drawableTint="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/close_icon" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/btnVideoHome"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@null"
            android:drawableTint="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/home_icon" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
